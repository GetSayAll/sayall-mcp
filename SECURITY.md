# Security

## 报告安全问题

请通过 GitHub Security Advisory 私下报告安全问题，不要在公开 Issue 中提交访问令牌、真实转写正文、client ID、用户目录路径或包含这些内容的日志。

## 安全边界

- MCP Helper 仅通过父进程提供的 stdin/stdout 通信，不监听网络端口。
- 本地 Agent 访问默认关闭，必须由用户在无线麦SayAll.app 内主动开启并为每个客户端单独授权。
- 明文访问令牌只在创建时显示一次；磁盘只保存 SHA-256 哈希。
- Helper 每次工具调用都会重新检查总开关、授权和撤销状态。
- 工具只读语音转文字历史，不提供写入、修改、删除或清空能力。
- 授权文件和审计文件拒绝符号链接与非普通文件。

本设计不能隔离同一 macOS 登录用户下的恶意非沙盒进程。被授权的第三方 AI 客户端也可能把返回文字发送给其云端服务；这属于对应客户端的数据处理边界。
